#include <bits/stdc++.h>
#define endl '\n'

using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    string line;
    int count = 0;

    while (getline(cin, line))
    {
        count++;
    }

    cout << count << endl;

    return 0;
}