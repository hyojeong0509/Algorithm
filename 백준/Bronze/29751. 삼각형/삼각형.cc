#include <bits/stdc++.h>
#define endl '\n'

using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int a, b;
    float c;

    cin >> a >> b;
    c = (a * b) / 2.0;

    cout << fixed << setprecision(1) << c << endl;

    return 0;
}